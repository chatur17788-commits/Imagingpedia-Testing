[variables]
NIXPACKS_NODE_VERSION = "22"

[phases.setup]
nixPkgs = ["nodejs_22", "npm-10_x"]

[phases.install]
cmds = ["cd \"src/Backed for AI Test/ai-lms-backend\" && npm install"]

[start]
cmd = "cd \"src/Backed for AI Test/ai-lms-backend\" && npm start"
